# .github/workflows/laravel.yml
name: Deploy Frontend to Render

on:
  push:
    branches: ["master"]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Trigger Render deployment
        env:
          RENDER_DEPLOY_HOOK: ${{ secrets.RENDER_DEPLOY_HOOK }}
        run: |
          if [ -z "$RENDER_DEPLOY_HOOK" ]; then
            echo "❌ RENDER_DEPLOY_HOOK secret is not set"
            exit 1
          fi
          HTTP_STATUS=$(curl -s -o /tmp/render_response.txt -w "%{http_code}" \
            -X POST -H "Content-Type: application/json" -d '{}' \
            "$RENDER_DEPLOY_HOOK")
          echo "HTTP $HTTP_STATUS — $(cat /tmp/render_response.txt)"
          if [ "$HTTP_STATUS" -ge 200 ] && [ "$HTTP_STATUS" -lt 300 ]; then
            echo "✅ Render build triggered successfully"
          else
            echo "❌ Deploy trigger failed (HTTP $HTTP_STATUS)"
            exit 1
          fi