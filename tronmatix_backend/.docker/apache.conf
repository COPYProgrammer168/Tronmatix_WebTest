<VirtualHost *:10000>
    ServerName tronmatix-beckend.onrender.com
    DocumentRoot /var/www/html/public

    # Trust Render's reverse proxy HTTPS header
    SetEnvIf X-Forwarded-Proto https HTTPS=on

    <Directory /var/www/html/public>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    # ── FIX: CORS headers on EVERY response (incl. 4xx/5xx) ──────────────────
    # "always" keyword = headers added even on error responses
    # Without these, browser blocks preflight and real requests
    Header always set Access-Control-Allow-Origin  "https://tronmatix-webtest.onrender.com"
    Header always set Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS"
    Header always set Access-Control-Allow-Headers "Authorization, Content-Type, Accept, Origin, X-Requested-With, X-CSRF-TOKEN"
    Header always set Access-Control-Expose-Headers "Authorization"
    Header always set Access-Control-Max-Age "86400"

    # ── FIX: Return 200 for OPTIONS preflight immediately (skip Laravel/PHP) ───
    RewriteEngine On
    RewriteCond %{REQUEST_METHOD} OPTIONS
    RewriteRule ^ - [R=200,L]

    # Security headers
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"

    # Log to stdout/stderr so Render dashboard shows logs
    ErrorLog  /dev/stderr
    CustomLog /dev/stdout combined
</VirtualHost>
